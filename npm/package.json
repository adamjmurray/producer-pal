{
  "name": "producer-pal",
  "version": "2.3.0-rc1",
  "description": "Stdio-to-HTTP bridge for Producer Pal MCP server",
  "homepage": "https://producer-pal.org",
  "type": "module",
  "bin": {
    "producer-pal": "./producer-pal-portal.js"
  },
  "files": [
    "producer-pal-portal.js",
    "licenses/",
    "LICENSE",
    "README.md",
    "producer-pal-logo.svg"
  ],
  "scripts": {
    "guard:no-prerelease": "node -e \"const v=require('./package.json').version; if (v.includes('-')) { console.error('\\n\\u274c Refusing to publish ' + v + ' to npm: -rcN builds are distributed as GitHub pre-releases only, and publishing one would move the npm latest tag. Promote it first: npm run version:bump:ga\\n'); process.exit(1) }\"",
    "prepublishOnly": "npm run guard:no-prerelease && cd .. && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/adamjmurray/producer-pal"
  },
  "keywords": [
    "music",
    "ableton",
    "ableton live",
    "mcp",
    "ai",
    "composition",
    "production"
  ]
}
